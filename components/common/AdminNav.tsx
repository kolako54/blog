import * as React from 'react';

interface IAppProps {
}

const App: React.FunctionComponent<IAppProps> = (props) => {
    return (
        <nav className='h-screen w-60'></nav>
  ) ;
};

export default App;
